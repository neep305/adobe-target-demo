extends layout

block content
    div(class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8")
        h1(class="text-3xl font-bold text-gray-900 mb-6") Adobe Analytics
        button(
            class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg shadow-sm transition duration-150 ease-in-out focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
            onclick="sendEvent()"
        ) Send Event

block scripts
    script.
        function sendEvent() {
            console.log("Sending event");
        }

